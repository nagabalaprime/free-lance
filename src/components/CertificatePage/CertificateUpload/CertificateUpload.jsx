import * as React from "react";
import {
    Footer
} from "@microsoft/azureportal-reactview/Footer";


/**
 * Upload certificate and get base 64 encoding as the output.
 */
export const CertificateUpload = React.memo(
  (props) => {
    const [errors, setErrors] = React.useState([]);

    return (
      <Footer
        
      />
    );
  }
);
